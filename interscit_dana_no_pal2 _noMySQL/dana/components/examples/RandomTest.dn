component provides App requires io.Output out, data.IntUtil iu, util.RandomInt ir {
	
	int App:main(AppParam params[])
		{
		ir.setSeed(1)
		
		for (int i = 0; i < 10; i++)
			{
			int rn = ir.get(10)
			out.println("rn:: $(iu.intToString(rn))")
			}
		
		return 0
		}
	}