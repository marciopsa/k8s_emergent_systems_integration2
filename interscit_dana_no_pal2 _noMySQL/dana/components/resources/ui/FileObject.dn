/*
{ "description" : "A graphical representation of a file or directory object, typically used by visual file browsers." }
*/

uses ClickableObject

interface FileObject extends ClickableObject{
	transfer char filePath[]
	
	FileObject(char path[])
	void setFile(char path[])
	char[] getFile()
	}